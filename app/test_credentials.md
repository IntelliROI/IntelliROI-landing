# Test Credentials
# Agent writes here when creating/modifying auth credentials (admin accounts, test users).
# Testing agent reads this before auth tests. Fork/continuation agents read on startup.

# IntelliROI landing page — NO AUTHENTICATION.
# Public demo-request form posts to POST /api/demo-requests (stored in MongoDB collection: demo_requests).
# List submissions: GET /api/demo-requests
