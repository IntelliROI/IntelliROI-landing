{
  "name": "intelliroi-landing-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix app run dev",
    "build": "npm --prefix app run build",
    "preview": "npm --prefix app run preview",
    "server": "uvicorn server:app --reload --port 8000"
  },
  "dependencies": {
    "framer-motion": "^12.43.0",
    "lenis": "^1.3.25"
  }
}
